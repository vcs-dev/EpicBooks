@model Domain.Negocio.Pedido
@{
    ViewData["Title"] = "Detalhes do Pedido";
}

<div class="container">
    <div class="row">
        <div class="col-md-3 pl-0">
            @await Html.PartialAsync("_menu")
        </div>
        <div class="col-md-9">
            <h4>Detalhes do pedido @Model.Id</h4>
            @await Html.PartialAsync("_pedido", Model)
        </div>
    </div>
</div> <!-- /container -->
