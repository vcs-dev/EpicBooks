@model Domain.DadosCliente.Endereco
@{
    ViewData["Title"] = "Endereço - Cadastro";
}

<div class="container">
    <div class="row">
        <div class="col-md-3 pl-0">
            @await Html.PartialAsync("_menu")
        </div>
        <div class="col-md-9">
            <h4>Adicionar endereço</h4>
            <form action="/Loja/MinhaConta/SalvarEndereco/" method="post">
                @await Html.PartialAsync("_endereco", Model)
                <button type="submit" class="btn btn-success mb-2">Salvar</button>
            </form>
        </div>
    </div>
</div> <!-- /container -->
@await Html.PartialAsync("_modalMensagem")

@section Scripts{
    <script src="~/js/loja/minhaconta/detalhes-endereco.js"></script>
}
