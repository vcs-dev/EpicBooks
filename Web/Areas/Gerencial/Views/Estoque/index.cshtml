@model IEnumerable<Domain.Negocio.Estoque>
@{
    ViewData["Title"] = "Estoque";
}

<h1>Estoque</h1>
@if (Model.Count() > 0)
{
    <div class="row mt-3 mb-3">
        <div class="form-group col-10">
            <input type="text" id="stringBusca" class="form-control" placeholder="Título">
        </div>
        <div class="col-2">
            <button id="btnConsultarProduto" class="btn btn-primary">Consultar</button>
        </div>
    </div>
}
@*<div class="row mt-3 mb-3">
    <div class="col-12">
        <a href="/Gerencial/Produtos/Cadastrar" class="btn btn-primary" title="">Cadastrar livro <i class="fa fa-plus"></i></a>
    </div>
</div>*@
<partial id="listaProdutos">
    @await Html.PartialAsync("_estoqueProds", Model)
</partial>
@await Html.PartialAsync("_modalMensagem", Model)
@section Scripts{
    <script src="~/js/gerencial/estoque/estoque.js"></script>
}

